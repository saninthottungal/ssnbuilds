class ProjectModel {
  const ProjectModel({
    required this.title,
    required this.description,
    required this.githubUrl,
  });

  final String title;
  final String description;
  final String githubUrl;
}
